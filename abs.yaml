# the conda build parameters to use
build_parameters:
  - "--suppress-variables"
  - "--skip-existing"
  - "--error-overlinking"
  - "--variants \"{skip_gpu: True}\""

# destination upon merge
upload_channels:
  ai-staging