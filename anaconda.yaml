anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: ggml-org/llama.cpp
      use_max_tag: true
      include_regex: ^b\d+$
      from_pattern: ^b(\d+)$
      to_pattern: 0.0.\1
    packages:
      - gguf
      - libllama
      - llama.cpp
      - llama.cpp-tests
      - llama.cpp-tools
